def fingerprint(server):
    s=server.lower()
    if "apache" in s: return "Apache"
    if "nginx" in s: return "Nginx"
    if "iis" in s: return "IIS"
    return server
def fingerprint(server):
    s=server.lower()
    if "apache" in s: return "Apache"
    if "nginx" in s: return "Nginx"
    if "iis" in s: return "IIS"
    return server

